import EncompassConnect from '../encompassConnect';

/**
 * @ignore
 */
class EncompassService {
  context: EncompassConnect;

  constructor(context: EncompassConnect) {
    this.context = context;
  }
}

export default EncompassService;
